Custom libvips build:

- libheif without AOM
- SVT-AV1 encoder
- DAV1D decoder
- optimized for sharp

# My Tap

## How do I install these formulae?

`brew install my/tap/<formula>`

Or `brew tap my/tap` and then `brew install <formula>`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "my/tap"
brew "<formula>"
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
